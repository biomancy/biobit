# bioproj Python bindings

This crate reserves the Python binding surface for `bioproj`. The initial
implementation exposes the Rust module namespace only; Python value bindings
will follow the released-provenance Rust API.
